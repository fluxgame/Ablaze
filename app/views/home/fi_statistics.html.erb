<p>Lean FI Date: <%= current_user.fi_date(current_user.aggregate_amounts[:lean_fi_expenses]) %></p>
<p>Full FI Date: <%= current_user.fi_date %></p>
<table>
	<thead>
		<tr>
			<th>Expense</th>
			<th>Recent Spending</th>
			<th>Recent Unplanned Spending</th>
			<th>Average Monthly Spending</th>
			<th>Available to Spend</th>
			<th>Available to Budget</th>
		</tr>
	</thead>
	<tbody>
		<% current_user.expense_accounts.each do |a| %>
		<tr>
			<td><%= a.name %></td>
			<td><%= current_user.home_asset_type.to_currency(a.last_months_spending(current_user.home_asset_type)) %></td>
			<td><%= current_user.home_asset_type.to_currency(a.recent_unplanned_spending(current_user.home_asset_type)) %></td>
			<td><%= current_user.home_asset_type.to_currency(a.average_monthly_spending(current_user.home_asset_type)) %></td>
			<td><%= current_user.home_asset_type.to_currency(a.available_to_spend(current_user.home_asset_type)) %></td>
			<td><%= current_user.home_asset_type.to_currency(a.available_to_budget(current_user.home_asset_type)) %></td>
		</tr>
		<% end %>
	</tbody>
</table>
