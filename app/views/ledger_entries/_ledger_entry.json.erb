{
"id": "<%= ledger_entry.id.to_s %>",
"account": <%= render ledger_entry.account %>,
"budgetGoal": <%= render ledger_entry.budget_goal.nil? ? BudgetGoal.new(user: current_user) : ledger_entry.budget_goal %>,
"amount": "<%= (ledger_entry.debit.nil? ? ledger_entry.credit : ledger_entry.debit) %>",
"debit": <%= ledger_entry.debit.nil? ? false : true %>
}