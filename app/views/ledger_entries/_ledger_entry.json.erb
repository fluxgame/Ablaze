{
"id": "<%= ledger_entry.id.to_s %>",
"account": <%= render 'accounts/account', account: ledger_entry.account, simple: true %>,
"budgetGoal": <%= render 'budget_goals/budget_goal', budget_goal: ledger_entry.budget_goal.nil? ? BudgetGoal.new(user: current_user) : ledger_entry.budget_goal, simple: true %>,
"amount": "<%= (ledger_entry.debit.nil? ? ledger_entry.credit : ledger_entry.debit) %>",
"debit": <%= ledger_entry.debit.nil? ? false : true %>
}