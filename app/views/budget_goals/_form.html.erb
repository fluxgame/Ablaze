<%= form_with(model: budget_goal, local: true) do |form| %>
  <% if budget_goal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(budget_goal.errors.count, "error") %> prohibited this budget_goal from being saved:</h2>

      <ul>
      <% budget_goal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :budget_goal_name %>
  </div>

  <div class="field">
    <%= form.label :remaining_amount %>
    <%= form.text_field :remaining_amount, id: :budget_goal_remaining_amount %>
  </div>

	<div class="field">
		<%= form.label :account_id %>
		<%= form.select(:account_id, Account.all.collect {|a| [a.name, a.id]}) -%>
	</div>
	
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
