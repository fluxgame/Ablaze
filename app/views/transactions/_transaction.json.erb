{
			"id": "<%= transaction.id.to_s %>",
			"description": "<%= transaction.description %>"<% if !local_assigns[:simple] %>,
			"masterLedgerEntry": <%= render transaction.master_ledger_entry, simple: true %>,
			"slaveLedgerEntries": [
				<%= transaction.slave_ledger_entries.map { |le| render le, simple: true }.join(", ").html_safe %>
			]<% end %>
}
